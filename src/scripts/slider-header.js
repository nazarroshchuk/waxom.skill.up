$('.header-slider').flickity({
    // options
    cellAlign: 'center',
    // contain: true,
    wrapAround: true,
    pageDots: true,
    arrowShape: {
        x0: 10,
        x1: 60, y1: 50,
        x2: 60, y2: 45,
        x3: 15
    }
});
